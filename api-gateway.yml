server:
  port: 8080

spring:
  application:
    name: api-gateway

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/tingeso
          #issuer-uri: http://keycloak:8090/realms/tingeso
          #jwk-set-uri: http://keycloak:8090/realms/tingeso/protocol/openid-connect/certs
    
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: 
              - "http://localhost:3000"
              - "http://frontend"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: client-service
          uri: lb://CLIENT-SERVICE
          predicates:
            - Path=/api/client/**
            
        - id: tool-service
          uri: lb://TOOL-SERVICE
          predicates:
            - Path=/api/tool/**
            - Path=/api/catalog/**
        
        
        # Agrega más rutas según tus microservicios
