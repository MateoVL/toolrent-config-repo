server:
  port: 0  # Puerto dinámico asignado por el sistema

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}
    
spring:
  application:
    name: fee-service
  
  #security:
  #  oauth2:
  #    resourceserver:
  #      jwt:
  #        issuer-uri: http://keycloak:8080/realms/tingeso

  
  datasource:
    url: jdbc:mysql://mysql-fee:3306/feedb
    username: mateo
    password: 4321
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

# Configuración de RestTemplate para comunicación entre microservicios
