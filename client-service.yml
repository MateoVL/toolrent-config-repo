server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql-client:3306/clientdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: client
    password: client123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
